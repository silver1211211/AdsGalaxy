import assert from"node:assert/strict";import{execFileSync}from"node:child_process";import{readFileSync}from"node:fs";import test from"node:test";
const client=process.env.MARIADB_TEST_CLIENT,password=process.env.MARIADB_TEST_PASSWORD,port=process.env.MARIADB_TEST_PORT||"3407",migration=readFileSync("db/migrations/20260802_0111_referral_cache_precision.sql","utf8");const sql=(input)=>execFileSync(client,["--host=127.0.0.1",`--port=${port}`,"--user=root",`--password=${password}`,"--batch","--skip-column-names"],{input,encoding:"utf8"}).trim();
test("cache precision migration preserves 0.005 exactly and is safely rerunnable",{skip:!client||!password},()=>{sql("DROP DATABASE IF EXISTS agx_cache_precision;CREATE DATABASE agx_cache_precision;USE agx_cache_precision;CREATE TABLE users(id INT PRIMARY KEY,total_referral_earnings DECIMAL(15,2) DEFAULT 0);INSERT INTO users VALUES(1,5.41);"+migration+migration+"USE agx_cache_precision;UPDATE users SET total_referral_earnings=5.39;UPDATE users SET total_referral_earnings=total_referral_earnings+.005 WHERE id=1;");assert.equal(sql("SELECT total_referral_earnings FROM agx_cache_precision.users WHERE id=1"),"5.39500000");assert.match(sql("SELECT COLUMN_TYPE FROM information_schema.COLUMNS WHERE TABLE_SCHEMA='agx_cache_precision' AND TABLE_NAME='users' AND COLUMN_NAME='total_referral_earnings'"),/decimal\(24,8\)/)});
