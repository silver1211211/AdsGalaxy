import { NextResponse } from "next/server";import type { RowDataPacket } from "mysql2/promise";import pool from "@/lib/db";import { getAuthenticatedUser,getAuthErrorStatus } from "@/lib/auth";
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){try{const user=await getAuthenticatedUser(request.headers.get("x-telegram-init-data"));const{id}=await params;const [rows]=await pool.query<Array<RowDataPacket&Record<string,unknown>>>(`SELECT COUNT(DISTINCT tp.id) placements,COALESCE(SUM(ts.impression_delta),0) impressions,COALESCE(SUM(tp.clicks),0) clicks,COALESCE(SUM(ts.publisher_amount),0) earnings FROM channels ch LEFT JOIN teaser_placements tp ON tp.channel_id=ch.id LEFT JOIN teaser_settlements ts ON ts.placement_id=tp.id WHERE ch.id=? AND ch.user_id=? AND ch.is_deleted=0 GROUP BY ch.id`,[id,user.id]);if(!rows[0])return NextResponse.json({error:"Not found"},{status:404});return NextResponse.json(rows[0]);}catch(error){return NextResponse.json({error:"Unable to load Teaser statistics"},{status:getAuthErrorStatus(error)});}}
